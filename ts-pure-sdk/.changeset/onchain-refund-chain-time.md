---
"@lendasat/lendaswap-sdk-pure": patch
---

Gate the on-chain Bitcoin refund on the chain's median time past instead of
the local clock, so refund availability matches what the chain actually
accepts.
