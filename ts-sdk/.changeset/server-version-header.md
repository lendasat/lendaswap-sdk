---
"@satora/swap": patch
---

Forward SDK API requests with the `x-satora-server-version` compatibility
header from the underlying pure SDK so the backend can enforce server/API
semver compatibility.
