---
"@satora/swap": minor
---

Lightning → EVM swaps: `createLightningToEvmSwap` delegation, tracker
mapping (EVM server leg, no client leg), and action derivation (no refund
action — the hold payment unwinds on its own).
